import { Box, styled } from '@mui/material'

export const LocalParticipantScreen = styled(Box)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '100%',
})
export const ParticipantScreen = styled(Box)({
  width: '100%',
  height: '50vh',
  marginBottom: '1rem',
})
